<form method="post" action="{{ path('cart_delete') }}" onsubmit="return confirm('Êtes vous sûr de vouloir vider le panier?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ 'toto') }}">
    <button class="btn btn-outline-danger mt-2">Vider le panier</button>
</form>
